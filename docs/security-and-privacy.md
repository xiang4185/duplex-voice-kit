# Security and privacy

The Showcase is local-only. It contains no credentials, production address, provider SDK, real audio, persistent database, or user identity. Limited privacy keeps the page browsable, blocks voice actions explicitly, and exposes a local re-authorization callback without a system permission prompt.
